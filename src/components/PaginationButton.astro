---
const {"@click": click, ":class": dynamicClass } = Astro.props;
---

<button class="flex items-center gap-x-2 py-2 px-2.5 text-sm border border-slate-200 rounded-sm transition-colors hover:border-slate-400"
        :class={`${dynamicClass} 'opacity-0 pointer-events-none' : 'opacity-100 pointer-events-auto'`}
        @click={click}><slot /></button>