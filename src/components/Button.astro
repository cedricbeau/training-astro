---
const {"@click": click, "class": buttonClass, ":class": dynamicClass, ":text": text, label } = Astro.props;
---

<button type="button"
        @click={click}
        class={buttonClass}
        :class={dynamicClass}>
    <span class="sr-only" :text={text}>{label}</span>
    <slot/>
</button>