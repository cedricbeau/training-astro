---
const {href, title, label} = Astro.props;
---

<a href={href}
   title={title}
   class="block mb-2 pb-2 font-semibold text-slate-500 border-b border-slate-600 hover:text-slate-300 hover:border-slate-400 transition-colors">{label}</a>